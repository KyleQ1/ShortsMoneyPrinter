# Secrets for OmegaMoneyPrinter. Copy to .env and fill in. .env is gitignored.
# Loaded automatically by app/config.py (does not override real env vars).

# AI video generation (Seedance via Replicate):
REPLICATE_API_TOKEN=

# Alternatives / other providers:
# FAL_KEY=
# OPENAI_API_KEY=
# PEXELS_API_KEY=
